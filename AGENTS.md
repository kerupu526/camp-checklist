# 프로젝트 작업 규칙

## 사용자 응대

- 사용자에게 항상 존댓말과 부드러운 어투로 답변한다.

## 버전 관리

- 프로젝트 버전은 Semantic Versioning(`MAJOR.MINOR.PATCH`) 규칙을 따른다.
- 버그를 한 번 수정할 때마다 `PATCH` 버전을 1 증가시킨다.
- 이전 버전과 호환되는 기능을 추가할 때는 `MINOR` 버전을 1 증가시키고 `PATCH`를 0으로 초기화한다.
- 이전 버전과 호환되지 않는 변경을 할 때는 `MAJOR` 버전을 1 증가시키고 `MINOR`와 `PATCH`를 0으로 초기화한다.
- 버그 수정 작업을 완료했다고 보고하기 전에 `gradle.properties`의 `mod_version`이 함께 변경되었는지 확인한다.

## 변경 범위

- 사용자가 별도로 요청하지 않는 한 `ViewModel`, `ProgressStore`, `Network` 및 완성된 모드 연동 백엔드의 동작을 변경하지 않는다.
- 문제의 근본 원인을 확인한 뒤 최소 범위로 수정하며, 임시 우회 코드나 탭별 하드코딩 복제로 증상을 숨기지 않는다.
- 기존 사용자 변경 사항과 무관한 파일은 수정하거나 되돌리지 않는다.

## LDLib2 UI

- UI의 시각적 구조, 크기, 배치, 스타일, sprite는 UI Editor에서 제작한 NBT가 소유한다. Java에서 화면을 재설계하거나 레이아웃 값을 덮어쓰지 않는다.
- 원본 UI Editor 파일은 특정 PC의 절대경로에 의존하지 않는다. 저장소의 `src/main/resources/assets/camp_checklist/resources/ui/checklist_ui.ui.nbt`를 실행·테스트의 기준 템플릿으로 삼고, 외부 UI Editor 원본이 필요할 때만 사용자가 로컬 경로를 별도로 지정한다.
- Java는 원본 템플릿의 요소를 선택하고 데이터와 이벤트를 바인딩하는 역할만 담당한다.
- 탭 구조와 목표 카드 객체는 UI 인스턴스당 한 번만 구성한다. 일반적인 상태 동기화에서는 기존 객체를 유지하고 텍스트, 진행률, 토글 상태 같은 값만 갱신한다.
- LDLib2 API 동작이 불명확하면 사용 중인 정확한 버전의 공식 문서와 로컬 JAR 구현을 확인한다.
- 공용 초기화 경로에 `net.minecraft.client.*` 참조가 유입되지 않도록 하여 dedicated server 클래스 로딩을 안전하게 유지한다.

## 검증

- 완료를 보고하기 전에 관련 테스트와 `gradlew clean build`를 실행하고 결과를 확인한다.
- UI 변경에는 최소한 탭 수와 등록 토폴로지, 탭별 데이터 격리, 카드 수, 값 갱신 시 객체 재사용을 검증하는 회귀 테스트를 포함한다.
- 가능한 경우 dedicated server와 개발 클라이언트를 실행하고 최신 로그에서 예외, 클래스 로딩 오류, UI 토폴로지 불일치를 확인한다.
- 실제 화면 조작이 가능한 환경에서는 탭 전환, 스크롤, 토글과 재오픈 동작까지 수동 확인한다.

## 현재 UI 결함 및 인수인계 기준

- 최근 0.2.0 UI에서 다음 결함이 보고되었다: ScrollerView가 세로 남은 영역을 끝까지 채우지 못함, 긴 설명이 중간에서 잘림, 카드 아래 다중 아이템 상세 영역의 디자인이 기존 Ore UI와 어울리지 않음.
- 긴 설명은 임의로 문장을 재조립하거나 중간에서 잘라 표시하지 않는다. 가로 영역 제약을 Layout/TextWrap 설정으로 해결하고, 전체 원문은 툴팁 또는 명시적인 확장 영역으로 보존한다.
- 다중 조건 목표의 상세 행은 기존 카드와 Ore UI 시각 언어를 유지하는 최소 확장으로 재설계한다. 문자열 원문, 현재값, 목표값은 ViewModel에서 전달된 값을 사용하며 UI에서 JSON을 해석하지 않는다.
- ScrollerView는 부모의 남은 세로 공간을 flex/grow 및 부모 제약으로 채우고, 카드 확장 시 내부 레이아웃이 다시 계산되는지 확인한다. 카드 위치를 Java에서 직접 이동하지 않는다.
- 다음 작업 세션은 위 결함의 원인을 먼저 재현·확인하고, UI Editor NBT와 Java 바인딩의 책임을 구분한 뒤 최소 범위로 수정한다. 수정 전후에 긴 설명, 다중 상세 행, 세로 스크롤, 탭 전환을 모두 수동 확인한다.
