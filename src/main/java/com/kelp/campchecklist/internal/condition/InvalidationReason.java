package com.kelp.campchecklist.internal.condition;

/** Diagnostic reason for an internal native-condition invalidation. */
public enum InvalidationReason {
    ADVANCEMENT,
    PLAYER_ROSTER,
    RELOAD,
    INTERNAL
}
