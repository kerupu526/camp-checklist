package com.kelp.campchecklist.api;

/** Addon contract major, not a wire/storage version. The 0.x mod API is still under development. */
public final class ChecklistApi {
    public static final int API_MAJOR = 1;
    private ChecklistApi() {}
}
